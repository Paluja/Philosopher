#include "../include/philo.h"

int main(int ac, char **av)
{
    if (ac == 5 || ac == 6)
    {
        printf("Todo OK");
        //parser_input

        //data_init

        //start_coocking

        //clean
    }
    else{
        error_exit("Bad arguments");
    }
    return (0);
}